"""Backend service layer for RepoForge.

The modules in this package are intentionally framework-light so they can be
used by FastAPI routes, background workers, or tests before the database layer
exists.
"""

